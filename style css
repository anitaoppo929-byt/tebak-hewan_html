body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    height: 100vh;
    overflow: hidden;
    background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
}

/* Menu Awal */
.menu-container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%,-50%);
    text-align: center;
    background: rgba(255,255,255,0.2);
    padding: 50px;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    backdrop-filter: blur(10px);
}
.menu-container h1 {
    font-size: 3em;
    color: #fff;
    margin-bottom: 40px;
    text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
}
.btn {
    display: block;
    width: 200px;
    margin: 20px auto;
    padding: 15px 0;
    font-size: 1.2em;
    color: #fff;
    background: linear-gradient(135deg, #6a11cb, #2575fc);
    border: none;
    border-radius: 50px;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
}
.btn:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.3); }

/* Game */
#gameContainer { display:none; padding:20px; text-align:center; }
#levelContainer { display:flex; justify-content:center; margin:10px 0; }
.level-circle { width:30px; height:30px; border-radius:50%; background-color:rgba(255,255,255,0.5); margin:0 5px; display:flex; justify-content:center; align-items:center; font-weight:bold; color:#333; transition:all 0.3s; }
.level-circle.active { background-color:#ffd700; color:#fff; box-shadow:0 0 8px #ffd700; }
#coinDisplay { text-align:center; font-size:18px; color:#fff; margin-bottom:10px; }
#clueButton { display:block; margin:10px auto; padding:8px 15px; font-size:16px; background-color:#ff69b4; color:#fff; border:none; border-radius:8px; cursor:pointer; font-weight:bold; transition:all 0.3s; }
#clueButton:hover { transform:scale(1.05); }
.animal-row { display:flex; justify-content:center; gap:20px; margin-bottom:15px; }
.animal-img { max-width:150px; border-radius:12px; box-shadow:0 4px 10px rgba(0,0,0,0.3); filter:blur(15px); transition:filter 0.3s ease; }
.animal-img.revealed { filter:blur(0); }
#timer { font-size:18px; margin-bottom:10px; color:red; }
input { padding:10px; font-size:16px; width:180px; text-align:center; border-radius:8px; border:2px solid #ccc; outline:none; margin:5px; }
input:focus { border-color:#ffd700; box-shadow:0 0 5px #ffd700; }
button.game-btn { padding:10px 20px; font-size:16px; border:none; border-radius:8px; cursor:pointer; transition:all 0.3s ease; font-weight:bold; margin:5px; }
button.game-btn:hover { transform:scale(1.05); }
#guessButton { background-color:#ff7f50; color:#fff; }
#resetButton { background-color:#32cd32; color:#fff; }
#result { margin-top:15px; font-size:18px; min-height:24px; color:#333; }

/* Game Over */
#gameOver { display:none; text-align:center; color:#fff; }
#gameOver h1 { font-size:36px; margin-bottom:20px; text-shadow:2px 2px 5px rgba(0,0,0,0.3); }
#gameOver button { padding:15px 35px; font-size:20px; border-radius:10px; background-color:#32cd32; color:#fff; border:none; cursor:pointer; transition:all 0.3s ease; }
#gameOver button:hover { transform:scale(1.1); }

/* Tampilan Selesai */
#gameCompleted { display:none; text-align:center; position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); color:#fff; }
#gameCompleted h1 { font-size:36px; text-shadow:2px 2px 5px rgba(0,0,0,0.3); }
#gameCompleted button { padding:15px 35px; font-size:20px; border-radius:10px; background-color:#32cd32; color:#fff; border:none; cursor:pointer; transition:all 0.3s ease; margin-top:15px; }
#gameCompleted button:hover { transform:scale(1.1); }
body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    height: 100vh;
    overflow: hidden;
    background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
}

/* Menu Awal */
.menu-container {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%,-50%);
    text-align: center;
    background: rgba(255,255,255,0.2);
    padding: 50px;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    backdrop-filter: blur(10px);
}
.menu-container h1 {
    font-size: 3em;
    color: #fff;
    margin-bottom: 40px;
    text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
}
.btn {
    display: block;
    width: 200px;
    margin: 20px auto;
    padding: 15px 0;
    font-size: 1.2em;
    color: #fff;
    background: linear-gradient(135deg, #6a11cb, #2575fc);
    border: none;
    border-radius: 50px;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
}
.btn:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.3); }

/* Game */
#gameContainer { display:none; padding:20px; text-align:center; }
#levelContainer { display:flex; justify-content:center; margin:10px 0; }
.level-circle { width:30px; height:30px; border-radius:50%; background-color:rgba(255,255,255,0.5); margin:0 5px; display:flex; justify-content:center; align-items:center; font-weight:bold; color:#333; transition:all 0.3s; }
.level-circle.active { background-color:#ffd700; color:#fff; box-shadow:0 0 8px #ffd700; }
#coinDisplay { text-align:center; font-size:18px; color:#fff; margin-bottom:10px; }
#clueButton { display:block; margin:10px auto; padding:8px 15px; font-size:16px; background-color:#ff69b4; color:#fff; border:none; border-radius:8px; cursor:pointer; font-weight:bold; transition:all 0.3s; }
#clueButton:hover { transform:scale(1.05); }
.animal-row { display:flex; justify-content:center; gap:20px; margin-bottom:15px; }
.animal-img { max-width:150px; border-radius:12px; box-shadow:0 4px 10px rgba(0,0,0,0.3); filter:blur(15px); transition:filter 0.3s ease; }
.animal-img.revealed { filter:blur(0); }
#timer { font-size:18px; margin-bottom:10px; color:red; }
input { padding:10px; font-size:16px; width:180px; text-align:center; border-radius:8px; border:2px solid #ccc; outline:none; margin:5px; }
input:focus { border-color:#ffd700; box-shadow:0 0 5px #ffd700; }
button.game-btn { padding:10px 20px; font-size:16px; border:none; border-radius:8px; cursor:pointer; transition:all 0.3s ease; font-weight:bold; margin:5px; }
button.game-btn:hover { transform:scale(1.05); }
#guessButton { background-color:#ff7f50; color:#fff; }
#resetButton { background-color:#32cd32; color:#fff; }
#result { margin-top:15px; font-size:18px; min-height:24px; color:#333; }

/* Game Over */
#gameOver { display:none; text-align:center; color:#fff; }
#gameOver h1 { font-size:36px; margin-bottom:20px; text-shadow:2px 2px 5px rgba(0,0,0,0.3); }
#gameOver button { padding:15px 35px; font-size:20px; border-radius:10px; background-color:#32cd32; color:#fff; border:none; cursor:pointer; transition:all 0.3s ease; }
#gameOver button:hover { transform:scale(1.1); }

/* Tampilan Selesai */
#gameCompleted { display:none; text-align:center; position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); color:#fff; }
#gameCompleted h1 { font-size:36px; text-shadow:2px 2px 5px rgba(0,0,0,0.3); }
#gameCompleted button { padding:15px 35px; font-size:20px; border-radius:10px; background-color:#32cd32; color:#fff; border:none; cursor:pointer; transition:all 0.3s ease; margin-top:15px; }
#gameCompleted button:hover { transform:scale(1.1); }
